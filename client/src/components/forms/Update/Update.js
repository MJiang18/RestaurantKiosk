import React from "react";
import { useNavigate, Link } from "react-router-dom";

//update is the shopping cart view
const Update = () => {
  return (
    <div>
      UPDATE DETAILS PAGE
      <Link to="/login">login</Link> ||
      <Link to="/home">Home</Link>
    </div>
  );
};

export default Update;
